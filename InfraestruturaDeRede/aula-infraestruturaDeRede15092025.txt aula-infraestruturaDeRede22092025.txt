📚 Protocolos de Rede – Explicação Detalhada
🔹 1. Protocolo

Definição: Conjunto de regras e padrões que determinam como os dispositivos se comunicam em uma rede.

Função:

Estabelece a sintaxe (formato das mensagens).

Define a semântica (significado dos dados).

Regula o controle (como a comunicação começa, mantém e termina).

Exemplos de protocolos:

📚 Passo a passo do protocolo em redes

Aplicação cria os dados

Ex.: navegador gera um pedido HTTP.

Encapsulamento em camadas

Transporte (TCP/UDP) → adiciona portas e confiabilidade.

Internet (IP) → adiciona endereços de origem/destino.

Enlace (Ethernet/Wi-Fi) → adiciona endereços MAC.

Transmissão física

Dados viram sinais elétricos, ópticos ou de rádio e seguem pelo cabo ou Wi-Fi.

Switch/Roteador encaminha

Switch olha MAC.

Roteador olha IP e manda pelo melhor caminho.

Recepção e decapsulamento

Cada camada retira seu cabeçalho até chegar aos dados originais.

Controle e confirmação

TCP confirma entrega (ACKs).

Se houver erro, pede retransmissão.

Encerramento

Quando termina, a conexão é fechada (ex.: TCP envia FIN).

🔹 HTTP / HTTPS

O que é:

HTTP (HyperText Transfer Protocol): protocolo usado para acessar páginas da web.

HTTPS (HTTP Secure): versão segura do HTTP, que usa criptografia SSL/TLS.

O que faz:

Permite que navegadores e servidores troquem informações (ex.: quando você abre o Google).

O HTTPS protege os dados contra espionagem e alterações (segurança).

Exemplo prático:

Quando você digita http://site.com → conexão sem segurança.

Quando você digita https://site.com → conexão segura (com cadeado no navegador).

🔹 SMTP / IMAP / POP3 (protocolos de e-mail)

SMTP (Simple Mail Transfer Protocol):

Usado para enviar e-mails do seu programa (Outlook, Gmail, etc.) para o servidor.

Funciona apenas no envio.

IMAP (Internet Message Access Protocol):

Usado para receber e-mails do servidor, mas mantém as mensagens no servidor.

Permite sincronizar e-mails entre vários dispositivos (celular, PC, tablet).

POP3 (Post Office Protocol v3):

Também usado para receber e-mails, mas baixa as mensagens para o computador e, geralmente, remove do servidor.

Mais antigo e menos usado hoje.

Exemplo prático:

Você escreve um e-mail → SMTP envia.

Você abre a caixa de entrada no celular → IMAP sincroniza com o servidor.

Se fosse POP3, o e-mail seria baixado no PC e poderia sumir do servidor.

🔹 FTP / SFTP

FTP (File Transfer Protocol):

Usado para transferir arquivos entre computadores (ex.: enviar arquivos para um servidor de site).

Não é criptografado → inseguro.

SFTP (SSH File Transfer Protocol):

Versão segura do FTP, usa criptografia via SSH.
SSH, ou Secure Shell, é um protocolo de rede que permite acesso remoto e seguro a computadores, garantindo a confidencialidade e integridade dos dados através de criptografia. Ele é usado para administrar servidores, executar comandos e transferir arquivos de forma segura, substituindo métodos inseguros como o Telnet. 
Protege login, senha e arquivos transmitidos.

Exemplo prático:

Um programador usa SFTP para subir o código do site para o servidor.

🔹 DHCP (Dynamic Host Configuration Protocol)

O que é: Protocolo usado para atribuir IPs automaticamente a dispositivos na rede.

O que faz:

Quando você conecta no Wi-Fi, o roteador (servidor DHCP) te dá automaticamente:

Endereço IP

Máscara de rede

Gateway (saída para internet)

DNS (traduz sites para IPs)

Exemplo prático:

Você conecta o celular no Wi-Fi → o DHCP atribui um IP automaticamente (ex: 192.168.0.15), sem precisar configurar manualmente.

Protocolo	Função📊
HTTP/HTTPS	Acessar páginas da web (HTTPS é seguro)
SMTP	Enviar e-mails
IMAP	Receber e-mails (mantém no servidor, sincroniza)
POP3	Receber e-mails (baixa e remove do servidor)
FTP	Transferir arquivos (sem segurança)
SFTP	Transferir arquivos com criptografia (seguro)
DHCP	Atribuir IPs automaticamente aos dispositivos



🔹 2. IP (Internet Protocol)

Definição: Protocolo responsável por endereçar e encaminhar pacotes entre dispositivos em uma rede.

Funções principais:

Endereçamento lógico: cada dispositivo tem um IP único (IPv4: 32 bits / IPv6: 128 bits).

Fragmentação e montagem de pacotes: divide mensagens grandes em pacotes menores e reagrupa no destino.

Roteamento: usa roteadores para escolher o melhor caminho até o destino.

Estrutura de um pacote IP:

Cabeçalho (Header): contém informações de origem, destino, TTL (Time To Live), protocolo usado.
é a sigla para "Time to Live" (Tempo de Vida) e refere-se a um valor que limita a existência de um pacote de dados numa rede, 
um registo num servidor DNS, ou o tempo de retenção em cache.

Dados (Payload): a mensagem real que está sendo enviada.

Limitação: o IP não garante entrega, apenas encaminha. A confiabilidade fica a cargo de protocolos como o TCP.



🔹 3. TCP/IP (Transmission Control Protocol / Internet Protocol)

Definição: É a pilha de protocolos que sustenta a Internet. Criada nos anos 70 pelo Departamento de Defesa dos EUA.

Camadas do modelo TCP/IP (4 camadas):

Aplicação → serviços e protocolos usados por programas (HTTP, FTP, DNS, SMTP).

Transporte → confiabilidade e controle da comunicação (TCP e UDP).
UDP é o acrónimo de User Datagram Protocol (Protocolo de Datagrama do Usuário), um protocolo de comunicação da internet 
que envia pacotes de dados (datagramas) sem estabelecer uma conexão prévia nem garantir a entrega, privilegiando a velocidade sobre a confiabilidade

Internet → endereçamento e roteamento (IP, ICMP, ARP).

Acesso à Rede → define como os dados são transmitidos fisicamente (Ethernet, Wi-Fi).

Função do TCP:

Garante entrega confiável (reenvio em caso de perda).

Organiza os pacotes na ordem correta.

Cria uma conexão virtual entre origem e destino (handshake 3 vias).

Função do IP:

Endereça e encaminha os pacotes.

Exemplo prático: quando você abre um site:

O navegador usa HTTP (camada de aplicação).

O TCP quebra a página em pacotes e garante entrega correta.

O IP coloca o endereço e envia os pacotes pela rede.

O Ethernet/Wi-Fi entrega fisicamente até o roteador.



🔹 4. Pacotes Novell (IPX/SPX)

Definição: Conjunto de protocolos usados principalmente em redes Novell NetWare (décadas de 80 e 90).

Estrutura:

IPX (Internetwork Packet Exchange):

Similar ao IP → fornece endereçamento lógico e roteamento.

Endereçamento baseado em número de rede + nó.

SPX (Sequenced Packet Exchange):

Similar ao TCP → garante entrega confiável e ordenada.

Funções:

Facilitar comunicação entre PCs em redes locais.

Suporte para compartilhamento de arquivos e impressoras em servidores NetWare.

Status atual: caiu em desuso no fim dos anos 90, substituído por TCP/IP, que se tornou padrão global.



🔹 5. Pacotes Apple (AppleTalk)

Definição: Conjunto de protocolos criado pela Apple (anos 80) para comunicação entre computadores Macintosh.

Funções:

Permitia configuração automática dos nós na rede.

Endereçamento dinâmico (os dispositivos escolhiam seus próprios endereços).

Suporte nativo para compartilhamento de arquivos e impressoras em redes Apple.

Protocolos internos:

DDP (Datagram Delivery Protocol): similar ao IP.

ATP (AppleTalk Transaction Protocol): similar ao TCP.

Status atual: descontinuado a partir do Mac OS X 10.6 (2009) em favor do TCP/IP, que é universal.

📊 Quadro Comparativo
Protocolo	Função	Similaridade	Status Atual
Protocolo	Regras de comunicação entre dispositivos	"Língua" da rede	Base de todas as redes
IP	Endereçamento e roteamento de pacotes	Como o número da casa	Ativo (IPv4/IPv6)
TCP/IP	Pilha de protocolos que move a Internet	Correio → IP (endereço), TCP (confirma entrega)	Padrão mundial
IPX/SPX	Comunicação em redes Novell (endereço + confiabilidade)	IP + TCP	Obsoleto
AppleTalk	Comunicação em redes Macintosh	IP + TCP	Obsoleto
